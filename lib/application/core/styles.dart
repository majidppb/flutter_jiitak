import 'package:flutter/material.dart';

final class AppStyles {
  AppStyles._();

  static const homeVPadding = EdgeInsets.symmetric(vertical: 15);
  static const homeHPadding = EdgeInsets.symmetric(horizontal: 15);
}
